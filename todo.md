added functionalities:

- able to access via bot command
- did phase 0 and phase 1

todo:

- [ ] phase 2: check if the server is online, by waiting around 70s? we should benchmark later..
- [ ] do the backend stuff
    - [ ] `$kurisu server start` should call `init_server_start()`
    - [ ] make it do shenanigans
    - [ ] ~~ask elitu for other emails~~ actually no, lets focus on just getting things work for now.
